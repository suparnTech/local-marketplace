// Quick script to clear cart in Redux
// Run this in the app console or create a button to clear cart

import { clearCart } from './src/store/slices/cartSlice';

// In your app, dispatch this action:
// dispatch(clearCart());

// Or add a "Clear Cart" button in the cart screen temporarily
